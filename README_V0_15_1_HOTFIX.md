# SportsCard Vault V0.15.1 Hotfix

Fix for Render startup error:
`ModuleNotFoundError: No module named 'app.image_storage'`

This patch includes all persistence modules required by V0.15, not only the files changed by the readiness update.
Upload the **contents** of this patch into the repository root and replace files with the same names.
No API keys are included.
